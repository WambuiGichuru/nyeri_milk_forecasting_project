# Nyeri County Milk Production Forecasting
### Faith Wambui Gichuru — SCT213-C002-0003/2022 — JKUAT

A comparative time-series forecasting study applying ARIMA, SARIMA, and Facebook Prophet
to both observed Nyeri County dairy data and a Denton-Cholette simulated monthly dataset,
deployed as an interactive Streamlit dashboard.

---

## Project Structure

```
nyeri-milk-forecasting/
│
├── data/
│   ├── 01_county_annual.csv          ← Dataset 1: 9 observed annual records
│   ├── 02_subcounty_population.csv   ← Dataset 2: 5yr x 8 sub-county records
│   └── 03_simulated_monthly.csv      ← Dataset 3: Generated by simulate.py
│
├── 01_data_preparation.py            ← CRISP-DM Phase 1 & 2: understand + prepare
├── 02_simulate.py                    ← CRISP-DM Phase 3: generate Dataset 3
├── 03_eda.py                         ← CRISP-DM Phase 4a: exploratory analysis
├── 04_models.py                      ← CRISP-DM Phase 4b: train all 3 models
├── 05_evaluate.py                    ← CRISP-DM Phase 5: compare + rank models
├── app.py                            ← CRISP-DM Phase 6: Streamlit dashboard
│
├── requirements.txt
└── README.md
```

---

## Git Setup — Step by Step

### 1. Install Git
Download from https://git-scm.com/downloads and install.
Verify: open terminal and type `git --version`

### 2. Configure Git (one time only)
```bash
git config --global user.name "Faith Gichuru"
git config --global user.email "your@email.com"
```

### 3. Create GitHub Repository
1. Go to https://github.com and sign in
2. Click the **+** icon → **New repository**
3. Name: `nyeri-milk-forecasting`
4. Set to **Public**
5. Check **Add a README file**
6. Click **Create repository**

### 4. Clone to your computer
```bash
# Replace YOUR_USERNAME with your actual GitHub username
git clone https://github.com/YOUR_USERNAME/nyeri-milk-forecasting.git
cd nyeri-milk-forecasting
```

### 5. Set up Python virtual environment
```bash
# Create the environment
python -m venv venv

# Activate it — Windows:
venv\Scripts\activate

# Activate it — Mac/Linux:
source venv/bin/activate

# Install dependencies
pip install -r requirements.txt
```

### 6. Run the pipeline in order
```bash
python 01_data_preparation.py    # Creates the two observed CSV files
python 02_simulate.py            # Generates 03_simulated_monthly.csv
python 03_eda.py                 # Prints EDA summary to terminal
python 04_models.py              # Trains all 3 models, saves results
python 05_evaluate.py            # Prints comparison table
streamlit run app.py             # Launches dashboard in browser
```

### 7. Daily Git workflow
```bash
# Start of every session — pull latest changes
git pull origin main

# After making changes — save your work
git add .
git commit -m "feat: describe what you changed"
git push origin main
```

### 8. Deploy to Streamlit Cloud (free)
1. Go to https://share.streamlit.io
2. Sign in with GitHub
3. Click **New app**
4. Repository: `nyeri-milk-forecasting`
5. Main file path: `app.py`
6. Click **Deploy**
7. Your live URL will be: `https://nyeri-milk-forecasting.streamlit.app`

---

## Useful Git commit message prefixes
- `feat:` — new feature or file
- `fix:` — bug fix
- `data:` — data changes
- `docs:` — README or documentation
- `style:` — dashboard visual changes
- `refactor:` — code cleanup without changing behaviour
